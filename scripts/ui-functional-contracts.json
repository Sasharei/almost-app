{
  "version": 1,
  "policy": "These contracts may only be weakened or removed after explicit user approval for the corresponding behavior change.",
  "contracts": [
    {
      "id": "main-tab-navigation",
      "description": "The main tab bar keeps routing through the existing tab-change handler.",
      "file": "App.js",
      "requiredFragments": [
        "<LiquidGlassTabBar",
        "onTabPress={handleTabChange}"
      ]
    },
    {
      "id": "global-fab-actions",
      "description": "Short tap, long press, record decision, new temptation, new goal, and quick custom-spend submission remain wired.",
      "file": "App.js",
      "requiredFragments": [
        "onPress={handleFabPress}",
        "onLongPress={handleFabLongPress}",
        "onPress={handleFabRecordDecision}",
        "onPress={handleFabNewTemptation}",
        "onPress={handleFabNewGoal}",
        "onSubmit={handleQuickCustomSubmit}",
        "onCancel={handleQuickCustomCancel}"
      ],
      "requiredPatterns": [
        "const fabMainIcon = useMemo\\(\\(\\) => \\{\\s*if \\(activeTab === \"feed\"\\) \\{.*?return \"\\+\";\\s*\\}, \\[activeTab, widgetCurrencyCode\\]\\);",
        "useEffect\\(\\(\\) => \\{\\s*setFabScrollHidden\\(false\\);\\s*fabMenuAnim\\.stopAnimation\\(\\);\\s*fabMenuAnim\\.setValue\\(0\\);\\s*setFabMenuVisible\\(false\\);\\s*\\}, \\[fabMenuAnim, setFabScrollHidden, viewportLayoutKey\\]\\);"
      ]
    },
    {
      "id": "feed-new-temptation-action",
      "description": "The regular Feed add button stays rendered beside the scrollable category rail and outside the optional coach-mark condition.",
      "file": "App.js",
      "requiredFragments": [
        ": onNewTemptation",
        "accessibilityLabel={t(\"fabNewTemptation\")}"
      ],
      "requiredPatterns": [
        "<View\\s*style=\\{\\[\\s*styles\\.feedCategoryToolbar[\\s\\S]*?<ScrollView[\\s\\S]*?<View\\s*style=\\{\\[\\s*styles\\.feedTemptationAddWrap"
      ]
    },
    {
      "id": "feed-home-hero-actions",
      "description": "The Home hierarchy redesign preserves Almi, level, streak, saving rating, Today tabs and swipe navigation, history, goal, daily reward, challenge, Budget, Subscriptions, category filters, and income-prompt actions.",
      "file": "App.js",
      "requiredFragments": [
        "onPress={onMascotPress}",
        "idleAnimationsEnabled={shouldAnimateVisibleHero}",
        "const shouldUseStaticPreview = !idleAnimationsEnabled;",
        "onPress={toggleHeroLevelExpanded}",
        "onPress={handleUsageStreakPress}",
        "onPress={() => savingsHeroRef?.current?.openSavingRatingModal?.()}",
        "onRecentEventsPress={openHistoryModal}",
        "onGoalJarPress={openGoalJarDetail}",
        "onDailyRewardClaim={onDailyRewardClaim}",
        "onActiveChallengePress={onActiveChallengePress}",
        "onPress={isBudgetPressable ? onBudgetHeroPress : undefined}",
        "onPress={isClone ? null : handleHomeSubscriptionPress}",
        "const handleHeroCarouselTabPress = useCallback(",
        "onPress={() => handleHeroCarouselTabPress(index)}",
        "onScrollBeginDrag={handleHeroCarouselScrollBegin}",
        "onMomentumScrollEnd={handleHeroCarouselScrollEnd}",
        "onPress={() => handleFeedCategoryFilterPress(option.id)}",
        "onPress={handleDayTwoIncomePromptPress}",
        "onPress={handleDayTwoIncomePromptDismiss}"
      ]
    },
    {
      "id": "goal-jar-fullscreen-physics",
      "description": "The fullscreen Goal Jar keeps its coins visible and starts sensor-driven physics only after the modal is presented while the app is active.",
      "file": "App.js",
      "requiredFragments": [
        "onShow={onShow}",
        "onShow={() => setFullscreenPresented(true)}",
        "const fullscreenPhysicsActive = visible && fullscreenPresented && fullscreenAppActive;",
        "const restingCoins = visible ? buildGoalJarPhysicsCoins(visualCoinItems) : [];",
        "if (!fullscreenPhysicsActive || !Accelerometer?.addListener) return undefined;"
      ]
    },
    {
      "id": "onboarding-goal-actions",
      "description": "Preset/custom goal selection, amount editing, custom-goal creation, and Continue remain available.",
      "file": "App.js",
      "requiredFragments": [
        "onToggle?.(goalId);",
        "onAmountChange?.(goalId, text)",
        "onPress={onCustomGoalCreate}",
        "onPress={handleSubmit}"
      ]
    },
    {
      "id": "profile-edit-settings-actions",
      "description": "Profile edit/save/cancel and Settings navigation remain wired.",
      "file": "App.js",
      "requiredFragments": [
        "const showProfileActions = isEditing || !!onReportsPress;",
        "{ id: \"overview\", label: t(\"profileOverviewTab\") },",
        "{ id: \"settings\", label: t(\"profileSettingsTab\") },",
        "{ id: \"history\", label: t(\"profileHistoryTab\") },",
        "onPress={() => setProfileSection(section.id)}",
        "onPress={isEditing ? onSaveEdit : onEditPress}",
        "onPress={onCancelEdit}",
        "onPress={() => setProfileSection(\"settings\")}",
        "onPress={() => onFieldChange?.(\"incomePayday\", day)}",
        "{t(\"incomePaydayValue\", { day: incomePayday })}",
        "onThemeToggle(option.id"
      ]
    },
    {
      "id": "temptation-card-actions",
      "description": "Save, spend, pause, amount confirmation, edit tap, goal/budget links, and swipe actions remain reachable while the card is restyled.",
      "file": "App.js",
      "requiredFragments": [
        "onPress={handleActionPress}",
        "onPress={() => handleAmountModalActionPress(\"save\")}",
        "onPress={() => handleAmountModalActionPress(\"spend\")}",
        "onPress={openPauseFreezePicker}",
        "onAction?.(actionType, item, { amountUSD: normalizedAmountUSD });",
        "onAction?.(action.type, item, action.options || null);",
        "onSwipeArchive(item);",
        "onSwipeDelete(item);",
        "onPress={handleAmountCategoryBudgetPress}",
        "onPress={handleAmountGoalPress}",
        "onPress={handleAmountTimerPress}"
      ]
    },
    {
      "id": "temptation-card-editor-actions",
      "description": "The temptation editor keeps every editable field, schedule control, save, cancel, delete, and goal-selection action wired through the existing handlers.",
      "file": "App.js",
      "requiredFragments": [
        "<TemptationEditSheet",
        "onChangeText={onEditEmojiChange}",
        "onChangeText={onEditTitleChange}",
        "onChangeText={onEditPriceChange}",
        "onChangeText={onEditDescriptionChange}",
        "onChange={onEditCategoryChange}",
        "onValueChange={onEditFrequencyChange}",
        "onCustomChange={onEditFrequencyCustomChange}",
        "onReminderTimeChange={onEditReminderTimeChange}",
        "onWeeklyDayChange={onEditWeeklyDayChange}",
        "onMonthlyDayChange={onEditMonthlyDayChange}",
        "onPress={onEditGoalSelect}",
        "onPress={onEditSave}",
        "onPress={onEditCancel}",
        "onPress={onEditDelete}"
      ]
    },
    {
      "id": "temptation-card-create-actions",
      "description": "The temptation creation modal keeps title, amount, emoji, category, frequency, schedule, add, and cancel behavior wired while its layout and keyboard handling are redesigned.",
      "file": "App.js",
      "requiredFragments": [
        "<QuickCustomModal",
        "onChangeText={(text) => onChange(\"title\", text)}",
        "onChangeText={(text) => onChange(\"amount\", text)}",
        "onChangeText={(text) => onChange(\"emoji\", limitEmojiInput(text))}",
        "onChange={(cat) => onChange(\"category\", cat)}",
        "onValueChange={(next) => onChange(\"frequency\", next)}",
        "onCustomChange={(next) => onChange(\"customFrequency\", next)}",
        "onReminderTimeChange={(next) => onChange(\"reminderTime\", next)}",
        "onWeeklyDayChange={(next) => onChange(\"frequencyWeeklyDays\", next)}",
        "onMonthlyDayChange={(next) => onChange(\"frequencyMonthlyDays\", next)}",
        "onPress={onCancel}",
        "onPress={() => onSubmit(data)}"
      ]
    },
    {
      "id": "coin-entry-modal-actions",
      "description": "Coin entry keeps dismiss, save, spend, manual amount, and slider behavior wired.",
      "file": "App.js",
      "requiredFragments": [
        "onPress={() => handleAction(\"spend\")}",
        "onPress={() => handleAction(\"save\")}",
        "onPress={handleDismiss}",
        "onPress={handleAmountDoubleTap}",
        "onAccessibilityTap={() => openManual(\"amount\")}",
        "onAccessibilityAction={handleCoinAccessibilityAction}",
        "sinceLastTap <= COIN_ENTRY_MANUAL_DOUBLE_TAP_MS",
        "setSelectedCategory(initialSelectedCategoryRef.current);",
        "const cardExitAnim = useRef(new Animated.Value(0)).current;",
        "Animated.timing(cardExitAnim, {",
        "onPress={closeManual}",
        "if (manualVisible) {",
        "{...panResponder.panHandlers}"
      ]
    },
    {
      "id": "tamagotchi-menu-actions",
      "description": "The Tamagotchi redesign preserves every care, reward, customization, navigation, gesture, and celebration action.",
      "file": "App.js",
      "requiredFragments": [
        "onRequestClose={closeTamagotchiOverlay}",
        "onMascotPress={openTamagotchiOverlay}",
        "onFeed={handleTycoonAutosaveHungryFeed}",
        "onPress={closeTamagotchiOverlay}",
        "{...tamagotchiCloseSwipeResponder.panHandlers}",
        "onPress={handleTamagotchiDailyRewardPress}",
        "onPress={handleTamagotchiDailySummaryPress}",
        "onPress={openSkinPicker}",
        "onPress={showTamagotchiImmunityHint}",
        "tamagotchiCleanPanResponder.panHandlers",
        "onPress={handleTamagotchiHourlyRewardClaim}",
        "onPress={() => handleTamagotchiTabPress(tab.id)}",
        "{renderTamagotchiTabContent()}",
        "feedTamagotchi(food.id);",
        "playWithTamagotchiToy(toy.id);",
        "buyTamagotchiCleanTool(tool.id);",
        "setTamagotchiCleanToolArmed(true);",
        "handleTamagotchiCleanStroke();",
        "disabled={tamagotchiIsFull}",
        "onPress={startParty}",
        "onRequestClose={closeSkinPicker}",
        "<TouchableWithoutFeedback onPress={closeSkinPicker}>",
        "onPress={() => handleSkinSelect(skin.id)}",
        "const skinLoadFailed = pending && skin.isRemote && skinLoadState === \"error\";",
        "showCatCustomizationPaywall"
      ],
      "requiredPatterns": [
        "const openSkinPicker = useCallback\\(\\(\\) => \\{[\\s\\S]*?setTamagotchiVisible\\(false\\);[\\s\\S]*?setSkinPickerVisible\\(true\\);",
        "const handleSkinSelect = useCallback\\([\\s\\S]*?if \\(!catCustomizationUnlocked\\) \\{[\\s\\S]*?logEvent\\(\"tamagotchi_skin_preview_paywall\"[\\s\\S]*?setSkinPickerVisible\\(false\\);[\\s\\S]*?setTimeout\\(showCatCustomizationPaywall, 180\\);"
      ]
    },
    {
      "id": "tamagotchi-menu-accessibility",
      "description": "The redesigned Tamagotchi menu keeps native control semantics, selected and disabled states, and an accessible alternative to the clean swipe gesture.",
      "file": "App.js",
      "requiredFragments": [
        "accessibilityLabel={t(\"tamagotchiOutfitsLabel\")}",
        "accessibilityLabel={t(\"dailySummaryBadgeCTA\")}",
        "accessibilityLabel={t(\"tamagotchiImmunityHintTitle\")}",
        "accessibilityLabel={t(\"tamagotchiHourlyRewardClaim\")}",
        "accessibilityRole=\"tab\"",
        "accessibilityState={{ selected: active }}",
        "accessibilityState={{ disabled: tamagotchiIsFull }}",
        "accessible={isTamagotchiCleanToolSelected}",
        "onAccessibilityTap={"
      ]
    },
    {
      "id": "tamagotchi-analytics",
      "description": "The Tamagotchi redesign keeps the established open, care, reward, party, customization, and skin-download analytics events.",
      "file": "App.js",
      "requiredFragments": [
        "logEvent(\"tamagotchi_pressed\")",
        "logEvent(\"tamagotchi_opened\")",
        "logEvent(\"tamagotchi_hourly_reward_claimed\"",
        "logEvent(\"tamagotchi_feed\"",
        "logEvent(\"tamagotchi_play\"",
        "logEvent(\"tamagotchi_clean_tool_bought\"",
        "logEvent(\"tamagotchi_clean\"",
        "logEvent(\"tamagotchi_party_started\"",
        "logEvent(\"tamagotchi_skin_preview_paywall\"",
        "logEvent(\"tamagotchi_skin_selected\"",
        "logEvent(\"tamagotchi_skin_assets_download_failed\""
      ]
    },
    {
      "id": "premium-commerce-actions",
      "description": "Plan selection, purchase, restore, manage subscription, terms, privacy, and close remain connected end to end.",
      "file": "App.js",
      "requiredFragments": [
        "onPlanSelect={handlePremiumPlanSelected}",
        "onPlanPress={handlePremiumPlanPress}",
        "onRestorePress={handlePremiumRestore}",
        "onManagePress={handleManageSubscriptionsOpen}",
        "onTermsPress={handleTermsLinkOpen}",
        "onPrivacyPress={handlePrivacyPolicyOpen}",
        "onClose={closePremiumPaywall}"
      ]
    },
    {
      "id": "premium-paywall-controls",
      "description": "The paywall still invokes purchase, selection, restore, manage, and legal callbacks.",
      "file": "src/components/PremiumPaywallModalV2.js",
      "requiredFragments": [
        "const PAYWALL_DISMISS_COOLDOWN_MS = 5000;",
        "const canDismiss = dismissible && dismissCooldownComplete;",
        "if (!canDismiss) return;",
        "onPlanPress(selectedPlan.id, { source: \"primary_button\" })",
        "onPlanSelect(plan.id, { source: \"plan_card\" })",
        "onRestorePress({ source: \"restore_button\" })",
        "onManagePress({ source: \"manage_button\" })",
        "onTermsPress({ source: \"terms_link\" })",
        "onPrivacyPress({ source: \"privacy_link\" })"
      ]
    },
    {
      "id": "premium-unlock-stories-entry",
      "description": "The post-purchase Premium overview stays dismissible from the final action, close control, and the native system-back path.",
      "file": "App.js",
      "requiredFragments": [
        "overlay?.type === \"premium_unlock\"",
        "onRequestClose={dismissOverlay}",
        "<PremiumUnlockStories",
        "t={t}",
        "language={language}",
        "playSound={playSound}",
        "onClose={dismissOverlay}"
      ]
    },
    {
      "id": "premium-unlock-stories-controls",
      "description": "Premium Stories preserve close and completion while adding reachable previous, next, tap, swipe, reduced-motion, and screen-reader behavior.",
      "file": "src/components/PremiumUnlockStories.js",
      "requiredFragments": [
        "onPress={handleClose}",
        "onPress={handlePrimaryAction}",
        "onPress={isRtl ? handleNext : handlePrevious}",
        "onPress={isRtl ? handlePrevious : handleNext}",
        "PanResponder.create({",
        "if (activeIndexRef.current === STORY_DEFINITIONS.length - 1)",
        "isLastStory || reduceMotion || screenReaderEnabled",
        "accessibilityRole=\"progressbar\""
      ]
    },
    {
      "id": "budget-routing-and-persistence",
      "description": "Budget premium access, transfer persistence, debt-plan persistence, proof picking, and analytics remain wired end to end.",
      "file": "App.js",
      "requiredFragments": [
        "case \"purchases\":",
        "onPress={handleBudgetAutoLocked}",
        "onTransfer={handleBudgetTransfer}",
        "onDebtSettingsSave={handleBudgetDebtSettingsSave}",
        "onPickProof={pickBudgetProofImage}",
        "debtPayments: [entry, ...normalized.debtPayments]",
        "savingsDeposits: [entry, ...normalized.savingsDeposits]",
        "logEvent(\"budget_transfer_confirmed\"",
        "logEvent(\"budget_debt_plan_saved\""
      ]
    },
    {
      "id": "budget-screen-actions",
      "description": "Savings/debt switching, transfer proof, setup/edit debt plan, submit, cancel, backdrop, and hardware-back actions remain reachable.",
      "file": "App.js",
      "requiredFragments": [
        "onPress={() => setPane(item.id)}",
        "onPress={openDebtSettings}",
        "openTransfer(pane === \"debt\" ? \"debt\" : \"savings\");",
        "onRequestClose={closeTransfer}",
        "onPress={closeTransfer}",
        "onPress={() => handlePickTransferProof(\"camera\")}",
        "onPress={() => handlePickTransferProof(\"library\")}",
        "onPress={submitTransfer}",
        "onRequestClose={closeDebtSettings}",
        "onPress={closeDebtSettings}",
        "onPress={submitDebtSettings}"
      ]
    },
    {
      "id": "budget-transfer-validation",
      "description": "Available-balance, debt-overpay, proof, OCR mismatch, APR, and persistence validation may not be weakened by visual work.",
      "file": "App.js",
      "requiredFragments": [
        "if (amountUSD > availableUSD + 0.01) {",
        "if (transferDraft.type === \"debt\" && debtRemainingUSD > 0 && amountUSD > debtRemainingUSD + 0.01) {",
        "if (!transferDraft.proofImageUri) {",
        "if (transferDraft.verifyingProof) {",
        "const proofAmountMismatch =",
        "proofImageUri: \"\"",
        "proofMatchedAmount: null",
        "Math.max(0, Math.min(99, parseNumberInputValue(debtDraft.apr) || 0))",
        "onTransfer?.({",
        "onDebtSettingsSave?.({ initialDebtUSD: amountUSD, aprPercent, minMonthlyPaymentUSD });"
      ]
    },
    {
      "id": "progress-navigation-and-notification-badges",
      "description": "Progress destinations, internal Rewards navigation, system Back, actionable challenge/reward badge derivation, and the combined bottom-tab badge remain wired.",
      "file": "App.js",
      "requiredFragments": [
        "const handleProgressSectionChange = useCallback((nextSection) => {",
        "onProgressSectionChange={handleProgressSectionChange}",
        "rewardBadgeCount={rewardsBadgeCount}",
        "challengeRewardBadgeCount={challengeRewardsBadgeCount}",
        "challengeRewardsBadgeCount={challengeRewardsBadgeCount}",
        "achievements.filter((reward) => reward.unlocked && !reward.claimed).length",
        "challengeList.filter((challenge) => challenge.canClaim).length",
        "if (activeTab === \"cart\" && progressHubPane === \"rewards\") {",
        "setProgressHubPane(\"progress\");"
      ]
    },
    {
      "id": "progress-challenge-actions",
      "description": "Challenge start, claim, cancel, confirmation, premium limits, reminders, persistence, analytics, and coin payout remain reachable after the Progress redesign.",
      "file": "App.js",
      "requiredFragments": [
        "onChallengeAccept={handleChallengeAccept}",
        "onChallengeClaim={handleChallengeClaim}",
        "onChallengeCancel={handleChallengeCancel}",
        "onPress: () => onChallengeAccept?.(challenge.id)",
        "onPress: () => onChallengeCancel?.(challenge.id)",
        "onChallengeClaim?.(challenge.id);",
        "scheduleChallengeReminders(challengeId, def, startedAt, expiresAt)",
        "ensurePremiumFeatureAccess(PREMIUM_FEATURE_KEYS.unlimitedChallenges);",
        "setHealthPoints((prev) => prev + rewardAmount);",
        "logEvent(\"challenge_started\"",
        "logEvent(\"challenge_claimed\"",
        "logEvent(\"challenge_cancelled\""
      ]
    },
    {
      "id": "progress-reward-actions",
      "description": "Unlocked rewards remain claimable from the Rewards destination and continue updating claim state, coins, history, sound, haptics, and analytics.",
      "file": "App.js",
      "requiredFragments": [
        "onRewardClaim={handleRewardClaim}",
        "onRewardClaim?.(reward);",
        "onPress={() => (isClaimable ? onRewardClaim?.(reward) : undefined)}",
        "setClaimedRewards((prev) => {",
        "setHealthPoints((prev) => prev + rewardAmount);",
        "logEvent(\"reward_claimed\"",
        "logHistoryEvent(\"reward_claimed\""
      ]
    },
    {
      "id": "progress-goal-actions",
      "description": "Goal creation, editing, active-goal selection, long press, deletion, and repeat flow remain reachable when Goals moves behind the Overview destination.",
      "file": "App.js",
      "requiredFragments": [
        "onRemoveWish={handleRemoveWish}",
        "onGoalLongPress={handleGoalLongPress}",
        "onGoalEdit={openGoalEditorPrompt}",
        "onSetActiveGoal={handleActiveGoalSelect}",
        "onCreateGoal={handleProgressCreateGoal}",
        "onDelete={() => onRemoveWish(wish.id)}",
        "onPress={() => onCreateGoal?.()}",
        "onLongPress={() => onSetActiveGoal?.(goalId)}"
      ]
    },
    {
      "id": "progress-insight-actions",
      "description": "Budget, subscription, income, weekly spend, free-day, rescue, and impulse-insight actions remain reachable when Insights moves behind Overview.",
      "file": "App.js",
      "requiredFragments": [
        "onFreeDayLog={handleLogFreeDay}",
        "onFreeDayRescue={handleFreeDayRescue}",
        "onBudgetAutoLocked={handleBudgetAutoLocked}",
        "onBudgetOverrideChange={handleBudgetOverrideChange}",
        "onBudgetOverrideReset={handleBudgetOverrideReset}",
        "onBudgetRemainingOverrideChange={handleBudgetRemainingOverrideChange}",
        "onIncomeEntryOpen={openIncomeEntryModal}",
        "onExtraIncomeEntryOpen={openExtraIncomeEntryModal}",
        "onWeeklySpendPress={openSpendBreakdown}",
        "const WeeklyCardWrap = onWeeklySpendPress ? Pressable : View;",
        "onPress: onWeeklySpendPress",
        "onPress={onBudgetAutoLocked}",
        "onSubscriptionCreateTemptation={handleSubscriptionQuickCustomCreate}",
        "onSubscriptionAutoImport={handleSubscriptionAutoImport}",
        "onSubscriptionPaymentAction={handleSubscriptionPaymentAction}",
        "onImpulseMapLockedPress={handleImpulseMapLockedPress}"
      ]
    },
    {
      "id": "progress-monthly-limits-actions",
      "description": "The Monthly limits redesign preserves editor entry, month selection, category history, slider and accessibility adjustment, exact input, reset, remaining edit, income entry, save, cancel, backdrop, and hardware-back behavior.",
      "file": "App.js",
      "requiredFragments": [
        "onPress={handleBudgetWidgetPress}",
        "openCategoryHistory(entry.id);",
        "onRequestClose={closeBudgetModal}",
        "onPress={() => dismissKeyboardOrRun(closeBudgetModal)}",
        "onPress={() => setBudgetModalMonthKey(monthKey)}",
        "onPress={handleBudgetModalRemainingPress}",
        "accessibilityRole=\"adjustable\"",
        "const absoluteX = Number.isFinite(pageX) ? pageX : moveX;",
        "activeTrackGeometryRef.current = grantGeometry;",
        "latestLimitValueRef.current = normalized;",
        "budgetDraftSessionMonthRef.current === budgetModalActiveMonthKey",
        "onAccessibilityAction={(event) => {",
        "onManualEdit={openBudgetManualPrompt}",
        "onResetLimit={handleBudgetCategoryReset}",
        "onValueChange={handleBudgetSliderDraftChange}",
        "onInteractionChange={handleBudgetSliderInteractionChange}",
        "onExtraIncomeEntryOpen?.(\"budget_modal\")",
        "onPress={confirmBudgetModal}",
        "onSubmitEditing={handleBudgetManualSubmit}",
        "onPress={handleBudgetManualSubmit}",
        "onPress={closeBudgetManualPrompt}"
      ]
    },
    {
      "id": "progress-balance-potential-actions",
      "description": "The Balance and potential redesign preserves disclosure, balance and forecast trends, category history, analytics, modal dismissal, and accessibility semantics.",
      "file": "App.js",
      "requiredFragments": [
        "onPress={toggleProgressAnalytics}",
        "onPress={openBalanceTrendModal}",
        "onPress={openPotentialTrendModal}",
        "logEvent(\"progress_analytics_opened\", { category_id: entry.id });",
        "openCategoryHistory(entry.id);",
        "onRequestClose={closePotentialTrendModal}",
        "onRequestClose={closeCategoryHistory}",
        "accessibilityState={{ expanded: progressAnalyticsExpanded }}"
      ]
    },
    {
      "id": "weekly-breakdown-modal-actions",
      "description": "The weekly savings and spending breakdown redesign preserves dismissal, range switching, period navigation, mode switching, and horizontally scrollable long ranges.",
      "file": "App.js",
      "requiredFragments": [
        "onRequestClose={closeBreakdown}",
        "<Pressable style={StyleSheet.absoluteFillObject} onPress={closeBreakdown} />",
        "onPress={closeBreakdown}",
        "onPress={() => setBreakdownRangeMode(tab.id)}",
        "onPress={handleBreakdownPrev}",
        "onPress={handleBreakdownNext}",
        "disabled={activeBreakdownOffset === 0}",
        "onPress={entry.onPress}",
        "disabled={isActive}",
        "ref={spendStatsScrollRef}"
      ]
    },
    {
      "id": "impulse-map-actions",
      "description": "The Impulse Map redesign preserves the Premium lock entry and the existing expand/collapse path while adding calendar drill-down interactions.",
      "file": "App.js",
      "requiredFragments": [
        "<ImpulseMapCard",
        "expanded={impulseExpanded}",
        "onToggle={handleImpulseToggle}",
        "onPress={() => setHeatMetric(option.id)}",
        "onPress={() => handleDayPress(day)}",
        "if (!expanded && typeof onToggle === \"function\") onToggle();",
        "<LockedImpulseMapPreview",
        "onPress={onImpulseMapLockedPress}"
      ]
    },
    {
      "id": "subscription-watch-actions",
      "description": "The Subscription Watch redesign keeps home and Insights entry points, automatic discovery, manual creation, modal dismissal, import confirmation, and open analytics wired to the existing behavior.",
      "file": "App.js",
      "requiredFragments": [
        "<HomeSubscriptionWidgetCard",
        "onPress={isClone ? null : handleHomeSubscriptionPress}",
        "onPress={openSubscriptionModal}",
        "onPress={() => handleSubscriptionAutoImportOpen(\"modal_button\")}",
        "onPress={handleSubscriptionCreateTemptation}",
        "onPress={handleSubscriptionAutoImportConfirm}",
        "onRequestClose={closeSubscriptionModal}",
        "onPress={closeSubscriptionModal}",
        "logEvent(\"subscription_watch_opened\""
      ]
    },
    {
      "id": "progress-bottom-tab-action-badge",
      "description": "The Progress bottom-tab badge remains the sum of actionable, unlocked challenge and reward notifications in native and fallback tab bars.",
      "file": "src/components/LiquidGlassTabBar.js",
      "requiredFragments": [
        "const challengeCount = challengesUnlocked ? Math.max(0, Number(challengeRewardsBadgeCount) || 0) : 0;",
        "const rewardCount = rewardsUnlocked ? Math.max(0, Number(rewardsBadgeCount) || 0) : 0;",
        "return challengeCount + rewardCount;",
        "badgeValue: badgeValue > 0 ? (badgeValue > 99 ? \"99+\" : String(badgeValue)) : null",
        "{badgeValue > 0 && ("
      ]
    },
    {
      "id": "progress-challenge-reward-persistence",
      "description": "Challenge and reward claims remain idempotent and persist challenge state, earned badges, claimed rewards, claim totals, and health coin balance.",
      "file": "App.js",
      "requiredFragments": [
        "if (!reward?.id || !reward.unlocked || reward.claimed) return;",
        "if (claimedRewardsRef.current?.[reward.id]) return;",
        "status: CHALLENGE_STATUS.CLAIMED",
        "claimedAt: Date.now()",
        "reminderNotificationIds: []",
        "queuePersist(STORAGE_KEYS.HEALTH, String(healthPoints));",
        "queuePersist(STORAGE_KEYS.CLAIMED_REWARDS, JSON.stringify(claimedRewards));",
        "queuePersist(STORAGE_KEYS.REWARD_TOTAL, String(Math.max(0, rewardClaimTotal)));",
        "queuePersist(STORAGE_KEYS.CHALLENGES, JSON.stringify(challengesState));",
        "queuePersist(STORAGE_KEYS.CHALLENGE_BADGES, JSON.stringify(challengeBadgeStore));"
      ]
    },
    {
      "id": "progress-overview-premium-insights-entry",
      "description": "The prominent Overview Insights entry always remains visible and opens the full Insights destination, where the existing premium gates remain wired.",
      "file": "App.js",
      "requiredFragments": [
        "onBudgetAutoLocked={handleBudgetAutoLocked}",
        "onProgressSectionChange={handleProgressSectionChange}",
        "const handleOverviewInsightsPress = useCallback(() => {",
        "onProgressSectionChange?.(\"insights\");",
        "onPress={handleOverviewInsightsPress}",
        "onPress={() => onProgressSectionChange?.(\"goals\")}",
        "ensurePremiumFeatureAccess(PREMIUM_FEATURE_KEYS.budgetAuto);",
        "ensurePremiumFeatureAccess(PREMIUM_FEATURE_KEYS.impulseMap);",
        "onPress={() => {\n            if (!activeGoalWish || !onGoalEdit) return;\n            onGoalEdit(activeGoalWish);"
      ]
    },
    {
      "id": "fridge-menu-actions",
      "description": "The Fridge redesign preserves open and close, edit, timer extension, spend, decline, overdue extension, swipe delete, Premium shelves, intro dismissal, and extension-modal actions.",
      "file": "App.js",
      "requiredFragments": [
        "onPress={handleToggleFridge}",
        "onOpenChange?.(false)",
        "onOpenChange?.(true)",
        "!(activeTab === \"pending\" && (!thinkingUnlocked || !fridgeDoorOpen))",
        "onPress={onEdit ? () => onEdit(item) : undefined}",
        "onPress={onExtend ? () => openFridgeExtendModal(item) : undefined}",
        "onPress={() => onResolve(item, \"spend\")}",
        "onPress={() => onResolve(item, \"decline\")}",
        "onPress={() => onExtend(item)}",
        "onDelete={onDelete ? () => onDelete(item) : undefined}",
        "onPress={onPremiumShelvesPress}",
        "onRequestClose={() => closeFridgeIntro(\"system_back\")}",
        "onPress={() => closeFridgeIntro(\"backdrop\")}",
        "onPress={() => closeFridgeIntro(\"cta\")}",
        "onRequestClose={() => closeFridgeExtendModal({ source: \"system_back\" })}",
        "onPress={() => closeFridgeExtendModal({ source: \"backdrop\" })}",
        "onPress={() => handleFridgeExtendOptionPress(option)}",
        "onPress={() => closeFridgeExtendModal({ source: \"cancel\" })}",
        "onPress={handleFridgeExtendConfirm}"
      ]
    },
    {
      "id": "fridge-item-create-actions",
      "description": "The Fridge item modal keeps dismiss, name, amount, emoji, freeze-duration, create, and edit-mode behavior wired to the existing callbacks.",
      "file": "App.js",
      "requiredFragments": [
        "function NewPendingModal({",
        "onRequestClose={onCancel}",
        "onPress={() => dismissKeyboardOrRun(onCancel)}",
        "onChangeText={(text) => onChange(\"title\", text)}",
        "onChangeText={(text) => onChange(\"amount\", text)}",
        "onChangeText={(text) => onChange(\"emoji\", limitEmojiInput(text))}",
        "!isEditingPending && (",
        "onChange(\"freezeOptionId\", option.id);",
        "onChange(\"freezeDurationMs\", option.durationMs);",
        "onPress={onCancel}",
        "onPress={onSubmit}"
      ]
    },
    {
      "id": "rating-prompt-actions",
      "description": "The localized store-rating prompt keeps backdrop/later dismissal and the native store-review confirmation action wired.",
      "file": "App.js",
      "requiredFragments": [
        "onPress={handleRatingPromptLater}",
        "onPress={handleRatingPromptConfirm}",
        "{t(\"ratingPromptLater\")}",
        "{t(\"ratingPromptAction\")}"
      ]
    },
    {
      "id": "ios-home-widget-actions-and-families",
      "description": "The iOS home widget redesign keeps all three WidgetKit families, the home destination, quick save and spend deep links, and the daily-summary action.",
      "file": "ios/Almost Widget/Almost_Widget.swift",
      "requiredFragments": [
        ".widgetURL(URL(string: \"com.sasarei.almostclean://home\"))",
        "Link(destination: URL(string: \"com.sasarei.almostclean://quick-entry?type=save\")!)",
        "Link(destination: URL(string: \"com.sasarei.almostclean://quick-entry?type=spend\")!)",
        "Link(destination: URL(string: \"com.sasarei.almostclean://daily-summary\")!)",
        ".supportedFamilies([.systemSmall, .systemMedium, .systemLarge])"
      ]
    },
    {
      "id": "android-home-widget-actions-and-sizes",
      "description": "The Android home widget redesign keeps small, medium, and large providers plus the existing home, quick-save, and quick-spend destinations.",
      "file": "android/app/src/main/java/com/sasarei/almostclean/widget/AlmostWidgetProvider.kt",
      "requiredFragments": [
        "ProviderEntry(AlmostWidgetSmallProvider::class.java, WidgetSize.SMALL)",
        "ProviderEntry(AlmostWidgetProvider::class.java, WidgetSize.MEDIUM)",
        "ProviderEntry(AlmostWidgetLargeProvider::class.java, WidgetSize.LARGE)",
        "Uri.parse(\"com.sasarei.almostclean://home\")",
        "Uri.parse(\"com.sasarei.almostclean://quick-entry?type=save\")",
        "Uri.parse(\"com.sasarei.almostclean://quick-entry?type=spend\")",
        "class AlmostWidgetSmallProvider : AppWidgetProvider()",
        "class AlmostWidgetLargeProvider : AppWidgetProvider()"
      ]
    }
  ]
}
